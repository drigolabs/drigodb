apiVersion: v1
kind: Namespace
metadata:
  name: drigodb-system
  labels: { app.kubernetes.io/part-of: drigodb }
---
# Hosted databases live apart from the control plane, so the API's RBAC is
# scoped to exactly one namespace and nothing else in the cluster.
apiVersion: v1
kind: Namespace
metadata:
  name: drigodb-databases
  labels: { app.kubernetes.io/part-of: drigodb }
