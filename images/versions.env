# The upstream versions the drigodb data-plane images are built from.
#
# Single source of truth: scripts/publish-images.sh sources this file and
# .github/workflows/images.yml reads it, so a version bump is one edit here and
# CI rebuilds, tests and publishes both images from it.
PG_MAJOR=18
DOCUMENTDB_VERSION=0.116-0
